SELECT
    SUM(text LIKE '%:scrooge:%') AS scrooge,
    SUM(text LIKE '%:bender:%') AS bender,
    SUM(text LIKE '%:fry:%') AS fry,
    SUM(text LIKE '%:getout:%') AS getout,
    SUM(text LIKE '%:chest:%') AS chest,
    SUM(text LIKE '%:slug:%') AS slug,
    SUM(text LIKE '%:homerdonut:%') AS homerdonut,
    SUM(text LIKE '%:canon:%') AS canon,
    SUM(text LIKE '%:dicaprio:%') AS dicaprio,
    SUM(text LIKE '%:wonder:%') AS wonder,
    SUM(text LIKE '%:tire:%') AS tire,
    SUM(text LIKE '%:vanga:%') AS vanga,
    SUM(text LIKE '%:bodyb:%') AS bodyb,
    SUM(text LIKE '%:tactics:%') AS tactics,
    SUM(text LIKE '%:runaway:%') AS runaway,
    SUM(text LIKE '%:ghost:%') AS ghost,
    SUM(text LIKE '%:pumpkin:%') AS pumpkin,
    SUM(text LIKE '%:flamer:%') AS flamer,
    SUM(text LIKE '%:plane:%') AS plane,
    SUM(text LIKE '%:axe:%') AS axe,
    SUM(text LIKE '%:sospfry:%') AS sospfry,
    SUM(text LIKE '%:insanepeka:%') AS insanepeka,
    SUM(text LIKE '%:awesome:%') AS awesome,
    SUM(text LIKE '%:search:%') AS search,
    SUM(text LIKE '%:metal:%') AS metal,
    SUM(text LIKE '%:psyduck:%') AS psyduck,
    SUM(text LIKE '%:huh:%') AS huh,
    SUM(text LIKE '%:toohot:%') AS toohot,
    SUM(text LIKE '%:lighter:%') AS lighter,
    SUM(text LIKE '%:yoyo:%') AS yoyo,
    SUM(text LIKE '%:taunt:%') AS taunt,
    SUM(text LIKE '%:vjuh:%') AS vjuh,
    SUM(text LIKE '%:snow:%') AS snow,
    SUM(text LIKE '%:charge:%') AS charge,
    SUM(text LIKE '%:buff:%') AS buff,
    SUM(text LIKE '%:sad:%') AS sad,
    SUM(text LIKE '%:notfunny:%') AS notfunny,
    SUM(text LIKE '%:plasma:%') AS plasma,
    SUM(text LIKE '%:police:%') AS police,
    SUM(text LIKE '%:megafon:%') AS megafon,
    SUM(text LIKE '%:selfie:%') AS selfie,
    SUM(text LIKE '%:wink:%') AS wink,
    SUM(text LIKE '%:bdbessa:%') AS bdbessa,
    SUM(text LIKE '%:dejavu:%') AS dejavu,
    SUM(text LIKE '%:flowers:%') AS flowers,
    SUM(text LIKE '%:dog2:%') AS dog2,
    SUM(text LIKE '%:ohmypeka:%') AS ohmypeka,
    SUM(text LIKE '%:ten:%') AS ten,
    SUM(text LIKE '%:bdcake:%') AS bdcake,
    SUM(text LIKE '%:kabluk:%') AS kabluk,
    SUM(text LIKE '%:nofeels:%') AS nofeels,
    SUM(text LIKE '%:wow:%') AS wow,
    SUM(text LIKE '%:sing:%') AS sing,
    SUM(text LIKE '%:sigh:%') AS sigh,
    SUM(text LIKE '%:orange:%') AS orange,
    SUM(text LIKE '%:thisisfine:%') AS thisisfine,
    SUM(text LIKE '%:detective:%') AS detective,
    SUM(text LIKE '%:spark:%') AS spark,
    SUM(text LIKE '%:goliath:%') AS goliath,
    SUM(text LIKE '%:bandages:%') AS bandages,
    SUM(text LIKE '%:winner:%') AS winner,
    SUM(text LIKE '%:pun:%') AS pun,
    SUM(text LIKE '%:kerrisad:%') AS kerrisad,
    SUM(text LIKE '%:kerrievil:%') AS kerrievil,
    SUM(text LIKE '%:marikbw:%') AS marikbw,
    SUM(text LIKE '%:overfan:%') AS overfan,
    SUM(text LIKE '%:waiting:%') AS waiting,
    SUM(text LIKE '%:buggy:%') AS buggy,
    SUM(text LIKE '%:loot:%') AS loot,
    SUM(text LIKE '%:archon:%') AS archon,
    SUM(text LIKE '%:rsp:%') AS rsp,
    SUM(text LIKE '%:bessa:%') AS bessa,
    SUM(text LIKE '%:peka:%') AS peka,
    SUM(text LIKE '%:scarypeka:%') AS scarypeka,
    SUM(text LIKE '%:pekaclap:%') AS pekaclap,
    SUM(text LIKE '%:pled:%') AS pled,
    SUM(text LIKE '%:rofl:%') AS rofl,
    SUM(text LIKE '%:daun:%') AS daun,
    SUM(text LIKE '%:omg:%') AS omg,
    SUM(text LIKE '%:smilegasm:%') AS smilegasm,
    SUM(text LIKE '%:happyface:%') AS happyface,
    SUM(text LIKE '%:cry:%') AS cry,
    SUM(text LIKE '%:sosp:%') AS sosp,
    SUM(text LIKE '%:shoked:%') AS shoked,
    SUM(text LIKE '%:panic:%') AS panic,
    SUM(text LIKE '%:rage:%') AS rage,
    SUM(text LIKE '%:cool:%') AS cool,
    SUM(text LIKE '%:geek:%') AS geek,
    SUM(text LIKE '%:kiss:%') AS kiss,
    SUM(text LIKE '%:sic:%') AS sic,
    SUM(text LIKE '%:yazik:%') AS yazik,
    SUM(text LIKE '%:zzz:%') AS zzz,
    SUM(text LIKE '%:drake:%') AS drake,
    SUM(text LIKE '%:divan:%') AS divan,
    SUM(text LIKE '%:whocares:%') AS whocares,
    SUM(text LIKE '%:verybad:%') AS verybad,
    SUM(text LIKE '%:exorcism:%') AS exorcism,
    SUM(text LIKE '%:poo:%') AS poo,
    SUM(text LIKE '%:fight:%') AS fight,
    SUM(text LIKE '%:respect:%') AS respect,
    SUM(text LIKE '%:boring:%') AS boring,
    SUM(text LIKE '%:homer:%') AS homer,
    SUM(text LIKE '%:kran:%') AS kran,
    SUM(text LIKE '%:dno:%') AS dno,
    SUM(text LIKE '%:jasonbobr:%') AS jasonbobr,
    SUM(text LIKE '%:joker:%') AS joker,
    SUM(text LIKE '%:heal:%') AS heal,
    SUM(text LIKE '%:evil:%') AS evil,
    SUM(text LIKE '%:sound:%') AS sound,
    SUM(text LIKE '%:badass:%') AS badass,
    SUM(text LIKE '%:darthbobr:%') AS darthbobr,
    SUM(text LIKE '%:jackie:%') AS jackie,
    SUM(text LIKE '%:muton:%') AS muton,
    SUM(text LIKE '%:charisma:%') AS charisma,
    SUM(text LIKE '%:fp:%') AS fp,
    SUM(text LIKE '%:gravitypeka:%') AS gravitypeka,
    SUM(text LIKE '%:slow:%') AS slow,
    SUM(text LIKE '%:racer:%') AS racer,
    SUM(text LIKE '%:flame:%') AS flame,
    SUM(text LIKE '%:fireext:%') AS fireext,
    SUM(text LIKE '%:hello:%') AS hello,
    SUM(text LIKE '%:brofist:%') AS brofist,
    SUM(text LIKE '%:love:%') AS love,
    SUM(text LIKE '%:fibo:%') AS fibo,
    SUM(text LIKE '%:valla:%') AS valla,
    SUM(text LIKE '%:reynor:%') AS reynor,
    SUM(text LIKE '%:grumpy:%') AS grumpy,
    SUM(text LIKE '%:whaaat:%') AS whaaat,
    SUM(text LIKE '%:whyyy:%') AS whyyy,
    SUM(text LIKE '%:doggie:%') AS doggie,
    SUM(text LIKE '%:nichobobr:%') AS nichobobr,
    SUM(text LIKE '%:bobrpirat:%') AS bobrpirat,
    SUM(text LIKE '%:cat:%') AS cat,
    SUM(text LIKE '%:thup:%') AS thup,
    SUM(text LIKE '%:squirrel:%') AS squirrel,
    SUM(text LIKE '%:dance:%') AS dance,
    SUM(text LIKE '%:onelove:%') AS onelove,
    SUM(text LIKE '%:tort:%') AS tort,
    SUM(text LIKE '%:gg:%') AS gg,
    SUM(text LIKE '%:delorian:%') AS delorian,
    SUM(text LIKE '%:dm:%') AS dm,
    SUM(text LIKE '%:goodnews:%') AS goodnews,
    SUM(text LIKE '%:karl:%') AS karl,
    SUM(text LIKE '%:gend:%') AS gend,
    SUM(text LIKE '%:crab:%') AS crab,
    SUM(text LIKE '%:gta:%') AS gta,
    SUM(text LIKE '%:bratok:%') AS bratok,
    SUM(text LIKE '%:toss:%') AS toss,
    SUM(text LIKE '%:roach:%') AS roach,
    SUM(text LIKE '%:zerg:%') AS zerg,
    SUM(text LIKE '%:marik:%') AS marik,
    SUM(text LIKE '%:dendi:%') AS dendi,
    SUM(text LIKE '%:gabe:%') AS gabe,
    SUM(text LIKE '%:moscow:%') AS moscow,
    SUM(text LIKE '%:lk:%') AS lk,
    SUM(text LIKE '%:bomb:%') AS bomb,
    SUM(text LIKE '%:sheep:%') AS sheep,
    SUM(text LIKE '%:acordion:%') AS acordion,
    SUM(text LIKE '%:muta:%') AS muta,
    SUM(text LIKE '%:rat:%') AS rat,
    SUM(text LIKE '%:bane:%') AS bane,
    SUM(text LIKE '%:rocket:%') AS rocket,
    SUM(text LIKE '%:rail:%') AS rail,
    SUM(text LIKE '%:shaft:%') AS shaft,
    SUM(text LIKE '%:gamepad1:%') AS gamepad1,
    SUM(text LIKE '%:gamepad2:%') AS gamepad2
FROM ggsite.ggchat_message WHERE timestamp > '2018-04-01' and timestamp < '2018-05-01'